datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model Order {
  id        Int      @id @default(autoincrement())
  name      String
  email     String
  service   String?
  message   String?
  createdAt DateTime @default(now())
}